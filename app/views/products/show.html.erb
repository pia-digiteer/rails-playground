<p style="color: green"><%= notice %></p>

<div class="d-flex flex-row justify-content-end align-items-center mb-3">
        <%= link_to "Edit this product", edit_product_path(@product), class: "btn btn-warning ms-4" %>
        <%= button_to "Destroy this product", @product, method: :delete, class: "btn btn-danger ms-4" %>
</div>

<%= render @product %>